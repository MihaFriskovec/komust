org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true

# Shared coordinates for every komust module (applied by the komust.kotlin-module
# convention plugin). The Gradle plugin's getPluginArtifact() and the engine fork
# both resolve `io.komust:*:${komustVersion}`.
komustGroup=io.komust
komustVersion=0.1.0-SNAPSHOT
